# Core agentic stack
langchain>=0.3
langchain-anthropic>=0.3
langgraph>=0.2
langsmith>=0.1

# MCP (Model Context Protocol) Python SDK — client + server
mcp>=1.0

# Utilities
python-dotenv>=1.0
httpx>=0.27
