# Dev-only tooling -- not needed to run the agent, only to develop it.
# Install with: .venv/bin/pip install -r requirements-dev.txt
# Config: ai-agent/mypy.ini (why it's needed: namespace packages here
# have no __init__.py, so mypy needs to be told ai-agent/ is the import
# root -- same thing Python's own sys.path[0] already gives it at
# runtime).
mypy>=1.0

# Test suite (ai-agent/tests/, see pytest.ini) -- pytest-asyncio for the
# async MCP tool functions in mcp_integrations/tools.py, respx to mock
# the httpx calls the MCP servers make to Prometheus/Actuator so unit
# tests don't need live infra.
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21
